export const metadata = {
  title: 'FaceBet',
  description: 'Bet Social Media',
}
 
export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
